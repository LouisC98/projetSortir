{% extends 'base.html.twig' %}

{% block title %}Votre lien de réinitialisation{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <div class="max-w-2xl mx-auto">
        <!-- En-tête -->
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">Demande de réinitialisation</h1>
            <p class="text-gray-600">Le processus de réinitialisation a été initié.</p>
        </div>

        <!-- Carte principale -->
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <div class="bg-gradient-to-r from-blue-50 to-indigo-50 px-6 py-4 border-b border-gray-200">
                <h2 class="text-lg font-semibold text-gray-900 flex items-center">
                    <svg class="w-5 h-5 mr-2 text-blue-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M13.19 8.688a4.5 4.5 0 011.242 7.244l-4.5 4.5a4.5 4.5 0 01-6.364-6.364l1.757-1.757m13.35-.622l1.757-1.757a4.5 4.5 0 00-6.364-6.364l-4.5 4.5a4.5 4.5 0 001.242 7.244" />
                    </svg>
                    Votre lien de réinitialisation
                </h2>
            </div>

            <div class="p-8">
                {% if resetUrl %}
                    <p class="text-gray-700 mb-4">Votre lien de réinitialisation a été généré avec succès. Copiez le lien ci-dessous et collez-le dans la barre d'adresse de votre navigateur.</p>
                    
                    <div class="bg-gray-100 border border-gray-200 rounded-lg p-4 mb-4">
                        <a href="{{ resetUrl }}" class="text-blue-600 break-all font-mono text-sm">{{ resetUrl }}</a>
                    </div>

                    <p class="text-sm text-gray-500">
                        Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                    </p>
                {% else %}
                    <p class="text-gray-700">
                        Si un compte correspondant à votre adresse e-mail existe dans notre système, un lien pour réinitialiser votre mot de passe a été généré. Pour des raisons de sécurité, nous ne pouvons pas confirmer si l'adresse e-mail a été trouvée ou non.
                    </p>
                {% endif %}
            </div>

            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 border-t border-gray-200">
                <div class="text-center">
                    <a href="{{ path('home') }}" class="font-medium text-blue-600 hover:text-blue-500 hover:underline transition-colors duration-200">
                        Retour à la page d'accueil
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}